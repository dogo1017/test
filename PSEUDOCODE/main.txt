Import all other files os, msvcrt

define function that pulls up menu (useable for all menu's in this file as well as in others) (parameters: )
- set cursor position to top option
- forever loop
    - clear the terminal
    - place cursor at selected position
    - get key press 
    - if key is a special character
        - get another key press
    - if key is up arrow
        - set index to itself but 1 lower
    - if key is down arrow
        - set index to itself but 1 bigger
    - if key is enter
        - stop function and return result     

define main function that controls the options between all files (no parameters)
- set selected_character to empty string
- set all options to all the different files
- forever loop
    - call menu function and set choice to itself
    - if choice is add_character
        - call add_character file
    - else if choice is skill_manager
        - check if there is a selected character
        - if there is
            - call skill_manager
        - if there isn't
            - return back to options
    - else if choice is inventory_manager
        - check if there is a selected character
        - if there is
            - call inventory_manager
        - if there isn't
            - return back to options
    - else if choice is attribute_manager
        - check if there is a selected character
        - if there is
            - call attribute_manager
        - if there isn't
            - return back to options
    - else
        - call search

call main