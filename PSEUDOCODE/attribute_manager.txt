
Define attribute management function with parameters charcters and selected character:
    - use menu from main to ask to select a desired attribute to modify or return to the main function
    - use menu to ask if they are sure, if no then ask if they want to manage their attributes again(ig yrs then restart character management function, if no return to main function)
    - if yes selected then use menu to display parts od attrubute to change like different values
    - enter new value amount or return to start of attirbbute management function
    - ask if they are sure, if no return to stat of function
    - if they are sure then applies changes to the variabe corresponding to that traits values
    - ask if the user wants to change more attributes or return
        - if more then return to start of attribute management function
        - if return than exit attribute management functions and return to main function
