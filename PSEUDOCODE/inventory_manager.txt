
define function to add inventory to selected character:
    - should all be in a while true loop
    - use menu function from main to choose between adding items to inventory, removing items from inventory, or return
    - if return chosen:
        - return back to main function by breaking
    - if add chosen:
        - display inventory of character previously chosen (e.g. "Your character already has in their inventory: blah, blah, blah...")
        - display options to add (e.g. "Available inventory additions: blah, blah, blah...")
            - each option should also have their stats (e.g. weight, effects, value, etc.)
        - after each addition to inventory, display message confirming addition (e.g. "Something has now been added to your inventory...")
        - also give option to just return to back to beginning of inventory management function
    if remove chosen:
        - display inventory of character previously chosen (e.g. "Your character already has in their inventory: blah, blah, blah...")
        - take user input of whatever they're tryna remove
            - each option should also have their stats (e.g. weight, effects, value, etc.)
        - after each addition to inventory, display message confirming addition (e.g. "Something has now been removed from your inventory...")
        - also give option to just return to back to beginning of inventory management function
